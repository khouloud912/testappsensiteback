class Employee {
    constructor(id, fullName, empCode, position, mobile ) {
            this.id = id;
            this.fullName = fullName;
            this.empCode = empCode;
            this.position = position;
            this.mobile = mobile;
    }
}

module.exports = Employee;